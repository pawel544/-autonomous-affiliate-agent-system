[project]
name = "autonomous-affiliate-agent-system"
version = "1"
description = "A MCP server project"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ "mcp>=1.27.1",]

[build-system]
requires = [ "uv_build>=0.11.15,<0.12.0",]
build-backend = "uv_build"

[project.scripts]
autonomous-affiliate-agent-system = "autonomous_affiliate_agent_system:main"
